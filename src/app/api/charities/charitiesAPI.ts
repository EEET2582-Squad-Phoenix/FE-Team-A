// import API from "@/utils/axiosClient";

// interface Charity {
//   id: string;
//   name: string;
// }


// export const fetchCharities = async (): Promise<Charity[]> => {
//   const response = await API.get<Charity[]>("/api/charities");
//   return response.data.map((charity) => ({
//     id: charity.id,
//     name: charity.name,
//   }));
// };
